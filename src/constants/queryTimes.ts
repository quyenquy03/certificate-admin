const STALE_TIME = {
  DEFAULT: 0,
  GET_MY_PROFILE: 2 * 60 * 60 * 1000,
};

const GC_TIME = {
  DEFAULT: 0,
  GET_MY_PROFILE: 2 * 60 * 60 * 1000,
};

export const QUERY_TIMES = {
  STALE_TIME,
  GC_TIME,
};

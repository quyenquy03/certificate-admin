const DEFAULT = {
  page: 1,
  limit: 10,
};

const GET_USERS = {
  page: 1,
  limit: 12,
};

const GET_ORGANIZATION_REGISTRATIONS = {
  page: 1,
  limit: 12,
};

const GET_CERTIFICATE_TYPES = {
  page: 1,
  limit: 12,
};

export const PAGINATION_PARAMS = {
  DEFAULT,
  GET_USERS,
  GET_CERTIFICATE_TYPES,
  GET_ORGANIZATION_REGISTRATIONS,
};
